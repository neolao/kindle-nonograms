# Kindle Nonograms — Codebase index
> Generated by /vibe:sync on 2026-08-16 (incremental). Re-run to update.

## Modules
- [`modules/shared.md`](modules/shared.md) — domain types and pure helpers shared by server and client
- [`modules/server.md`](modules/server.md) — Express API for the game
- [`modules/client.md`](modules/client.md) — vanilla TypeScript frontend built with Vite

## Observed patterns
- The Express app is built by a factory function (`createApp`) separate from the code that starts listening, so tests can exercise it without opening a port.
- Client code guards DOM access behind `typeof document !== "undefined"` so pure logic stays testable outside a browser.
- Domain types live in `shared` and are meant to be imported by both `server` and `client`.
- Clues are never stored — they are always derived on demand from a puzzle's solution grid.
- Browser-API-dependent client tests opt into a per-file `// @vitest-environment jsdom` pragma rather than switching the whole suite's default environment.

## Other context files
- [`models.md`](models.md) — data models
- [`glossary.md`](glossary.md) — ubiquitous language
