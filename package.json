{
  "name": "kindle-nonograms",
  "version": "1.5.0",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "vitest run",
    "lint": "biome check --write .",
    "build": "npm run build --workspaces --if-present",
    "dev:client": "npm run dev --workspace packages/client"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
